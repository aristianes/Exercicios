/**
 * Fazer um progama que leia um número e depois repita a quantidade de vezes o valor dele
 * 
 * para cada repetição escrever "O valor dessa iteração é:  + numero"
 */

/*

let numero;

numero = 60;

for (let i = 0; i < numero; i++) {
    if (i % 2 === 0) {
        console.log("Esse valor é par : " + i);
    } else {
        console.log(" Esse numero é impar: " + i);
    }
}
*/

let number = 20;

for (let i = 0; i < number; i++) {
    if (i % 2 === 0) {
        console.log("Numero par : " + i);
    } else {
        console.log("Numero impar: " + i);
    }
}